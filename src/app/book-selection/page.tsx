'use client';

export default function BookSelection() {
  return (
    <div className="container max-w-full flex min-h-screen flex-col items-center justify-center p-4">
        <h1 className="text-2xl font-bold mb-4">Book Selection Page</h1>
        <p>Select your book from the list below:</p>
        {}
    </div>
  );
}